<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent"/> <!-- Nền trong suốt -->
    <stroke android:width="1dp" android:color="@color/button_green_text_color"/> <!-- Viền màu xanh lá cây -->
    <corners android:radius="20dp"/> <!-- Bán kính bo tròn cao để có hình dạng viên thuốc -->
</shape>